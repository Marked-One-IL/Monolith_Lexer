import std

def main(argc: int32, argv: ptr[int8, 2]):

	if argc != 2:
		std.println("Argc count is not valid\nCount: ", argc)
		return 1

	limit: uint64 = std.strlen(argv[1])
	for i: uint64 = 0, i < limit, i++:
		std.println(argv[1][i])

	return 0
